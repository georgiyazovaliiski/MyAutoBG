@{
    ViewBag.Title = "Твоите поръчки";
}
@model Dictionary<AutoPartsMVC.Models.Order, List<AutoPartsMVC.Models.Product>>
        <div class="col-md-12 col-md-offset-1">
            <div class="panel panel-default panel-table">
                <div class="panel-heading">
                    <div class="row">
                        <div class="col col-xs-6">
                            <h3 class="panel-title">Твоите поръчки</h3>
                        </div>
                    </div>
                </div>
                <div class="panel-body">
                    <table class="table table-striped table-bordered table-list">
                        <thead>
                            <tr>
                                <th><em class="fa fa-cog"></em> Състояние</th>
                                <th>Номер на поръчката</th>
                                <th>Име</th>
                                <th>Телефон</th>
                                <th>Адрес</th>
                                <th>Дата на поръчката</th>
                                <th>Продукти</th>
                                <th>Цена</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach(var i in Model.Keys) { 
                            <tr>
                                <td align="center">
                                    @if (i.DateOfOrderFinish > new DateTime())
                                    {
                                        <span>Завършена</span>
                                    }
                                    else
                                    {
                                        <span>В изчакване</span>
                                    }
                                </td>
                                <td>@i.Id</td>
                                <td>@i.Name</td>
                                <td>@i.Phone</td>
                                <td>@i.Address</td>
                                <td>@i.DateOfOrderStart</td>
                                <td>@Model[i].Count (Прегледай)</td>
                                <td>@Model[i].Sum(a => a.Price) лв.</td>
                            </tr>
                            }
                        </tbody>
                    </table>
                    <br />
                    <p>
                        За отмяна или промяна на поръчка можете да се свържете с нас, чрез номера намиращ се <a href="@Url.Action("Index","Contacts")">тук</a>.
                    </p>
                </div>
            </div>
        </div>